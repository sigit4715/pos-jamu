@extends('layouts.app')
@section('title', 'Laporan Stok')
@section('heading', 'Laporan Stok')
@section('content')
<form class="card mb-6 flex gap-3" method="GET"><input class="input flex-1" name="q" value="{{ request('q') }}" placeholder="Cari nama barang"><button class="btn-secondary" type="submit">Cari</button></form>
<div class="grid gap-4 md:grid-cols-3 mb-6"><div class="stat-card"><p class="text-sm text-slate-500">Stok menipis</p><p class="mt-2 text-2xl font-black text-amber-600">{{ $lowCount }}</p></div><div class="stat-card"><p class="text-sm text-slate-500">Nilai modal stok</p><p class="mt-2 text-xl font-black">Rp {{ number_format($totalBuyValue, 0, ',', '.') }}</p></div><div class="stat-card"><p class="text-sm text-slate-500">Nilai jual stok</p><p class="mt-2 text-xl font-black text-emerald-600">Rp {{ number_format($totalSaleValue, 0, ',', '.') }}</p></div></div>
<div class="card overflow-x-auto p-0"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="p-4">Kode</th><th class="p-4">Barang</th><th class="p-4">Kategori</th><th class="p-4">Stok</th><th class="p-4">Minimum</th><th class="p-4">Modal</th><th class="p-4">Nilai stok</th></tr></thead><tbody>@forelse($products as $product)<tr class="border-t {{ $product->stock <= $product->minimum_stock ? 'bg-amber-50/50' : '' }}"><td class="p-4">{{ $product->code }}</td><td class="p-4 font-bold">{{ $product->name }}</td><td class="p-4">{{ $product->category?->name ?? '-' }}</td><td class="p-4 font-black">{{ $product->stock }} {{ $product->unit }}</td><td class="p-4">{{ $product->minimum_stock }}</td><td class="p-4">Rp {{ number_format($product->buy_price, 0, ',', '.') }}</td><td class="p-4">Rp {{ number_format($product->stock * $product->buy_price, 0, ',', '.') }}</td></tr>@empty<tr><td colspan="7" class="p-8 text-center text-slate-500">Belum ada barang aktif.</td></tr>@endforelse</tbody></table></div>
@endsection
