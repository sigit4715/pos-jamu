@extends('layouts.app')
@section('title', 'Penerimaan Transfer')
@section('heading', 'Penerimaan Transfer')
@section('content')
<div class="dashboard-toolbar"><div class="dashboard-welcome"><h2>Transfer masuk {{ $store->name }}</h2><p>Konfirmasi barang yang datang untuk menambah stok toko.</p></div><span class="status-badge status-waiting">{{ $pendingTransfers->count() }} menunggu</span></div>
<div class="space-y-4">@forelse($pendingTransfers as $transfer)<article class="card border-amber-200"><div class="flex flex-wrap items-start justify-between gap-3"><div><span class="status-badge status-waiting">Menunggu penerimaan</span><h3 class="mt-2 font-extrabold text-slate-800">{{ $transfer->number }}</h3><p class="mt-1 text-xs text-slate-500">Dari {{ $transfer->sourceStore->name }} oleh {{ $transfer->user->name }} · {{ $transfer->transferred_at->format('d M Y H:i') }}</p></div><form method="POST" action="{{ route('stock-transfers.receive', $transfer) }}">@csrf<button class="btn-primary" type="submit">Konfirmasi Diterima</button></form></div><div class="mt-4 grid gap-2 border-t border-slate-100 pt-3 sm:grid-cols-2">@foreach($transfer->items as $item)<div class="rounded-lg bg-slate-50 px-3 py-2 text-sm"><b>{{ $item->product_name }}</b><span class="float-right font-bold text-emerald-600">{{ $item->quantity }} {{ $item->unit_name }}</span><small class="mt-1 block text-slate-400">{{ $item->base_quantity }} stok dasar</small></div>@endforeach</div></article>@empty<div class="card text-center text-slate-500">Tidak ada transfer yang menunggu penerimaan.</div>@endforelse</div>
<section class="card mt-5 overflow-x-auto p-0"><div class="border-b p-5"><h3 class="font-extrabold">Transfer terbaru yang diterima</h3></div><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="p-4">Nomor</th><th class="p-4">Dari</th><th class="p-4">Barang</th><th class="p-4">Status</th></tr></thead><tbody>@forelse($receivedTransfers as $transfer)<tr class="border-t"><td class="p-4 font-bold">{{ $transfer->number }}</td><td class="p-4">{{ $transfer->sourceStore->name }}</td><td class="p-4">{{ $transfer->items->pluck('product_name')->join(', ') }}</td><td class="p-4"><span class="status-badge status-success">Diterima</span></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-500">Belum ada transfer diterima.</td></tr>@endforelse</tbody></table></section>
@endsection
