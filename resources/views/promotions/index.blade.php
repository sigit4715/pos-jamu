@extends('layouts.app')
@section('title', 'Promo dan Harga Khusus')
@section('heading', 'Promo & Harga Khusus')
@section('content')
<div class="mb-6"><h2 class="text-xl font-black">Promo otomatis di kasir</h2><p class="text-sm text-slate-500">Promo produk akan dihitung otomatis saat transaksi berlangsung.</p></div>
<form class="card mb-6" method="POST" action="{{ route('promotions.store') }}">@csrf<div class="grid gap-4 md:grid-cols-3"><div><label class="label">Nama promo</label><input class="input" name="name" required placeholder="Diskon jamu herbal"></div><div><label class="label">Produk</label><select class="input" name="product_id"><option value="">Semua produk</option>@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}</option>@endforeach</select></div><div><label class="label">Jenis</label><select class="input" name="type"><option value="percentage">Persentase (%)</option><option value="fixed">Potongan nominal</option></select></div><div><label class="label">Nilai promo</label><input class="input" name="value" type="number" min="0" step="0.01" required></div><div><label class="label">Mulai</label><input class="input" name="starts_at" type="datetime-local"></div><div><label class="label">Selesai</label><input class="input" name="ends_at" type="datetime-local"></div></div><label class="mt-4 flex items-center gap-2 text-sm"><input type="checkbox" name="is_active" value="1" checked> Promo aktif</label><button class="btn-primary mt-4" type="submit">Tambah Promo</button></form>
<div class="card overflow-x-auto p-0"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="p-4">Nama</th><th class="p-4">Produk</th><th class="p-4">Nilai</th><th class="p-4">Periode</th><th class="p-4">Status</th><th class="p-4"></th></tr></thead><tbody>@forelse($promotions as $promotion)<tr class="border-t"><td class="p-4 font-bold">{{ $promotion->name }}</td><td class="p-4">{{ $promotion->product?->name ?? 'Semua produk' }}</td><td class="p-4">{{ $promotion->type === 'percentage' ? $promotion->value.'%' : 'Rp '.number_format($promotion->value, 0, ',', '.') }}</td><td class="p-4">{{ $promotion->starts_at?->format('d M Y H:i') ?? '-' }} s/d {{ $promotion->ends_at?->format('d M Y H:i') ?? '-' }}</td><td class="p-4"><span class="rounded-full px-2 py-1 text-xs font-bold {{ $promotion->is_active ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-600' }}">{{ $promotion->is_active ? 'Aktif' : 'Nonaktif' }}</span></td><td class="p-4">@if($promotion->is_active)<form method="POST" action="{{ route('promotions.destroy', $promotion) }}">@csrf @method('DELETE')<button class="font-bold text-red-600" onclick="return confirm('Nonaktifkan promo ini?')">Nonaktifkan</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-slate-500">Belum ada promo.</td></tr>@endforelse</tbody></table></div>
@endsection
