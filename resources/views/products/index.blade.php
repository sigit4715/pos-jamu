@extends('layouts.app') @section('title','Master Barang') @section('heading','Master Barang') @section('content')<div class="mb-5 flex flex-wrap justify-between gap-3"><form class="flex gap-2"><input class="input w-72" name="search" value="{{ request('search') }}" placeholder="Cari nama, kode, barcode"><button class="btn-muted">Cari</button></form><a class="btn-primary" href="{{ route('products.create') }}">+ Tambah Barang</a></div><div class="card overflow-x-auto p-0"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-slate-500"><tr><th class="p-4">Barang</th><th class="p-4">Kategori</th><th class="p-4">Harga beli</th><th class="p-4">Harga jual</th><th class="p-4">Stok / Min.</th><th class="p-4">Status</th><th class="p-4"></th></tr></thead><tbody>@forelse($products as $product)<tr class="border-t border-slate-100"><td class="p-4"><b>{{ $product->name }}</b><small class="mt-1 block text-slate-500">{{ $product->code }} @if($product->barcode) · {{ $product->barcode }} @endif</small></td><td class="p-4">{{ $product->category?->name ?? '-' }}</td><td class="p-4">Rp {{ number_format($product->buy_price,0,',','.') }}</td><td class="p-4">Rp {{ number_format($product->price,0,',','.') }}</td><td class="p-4"><span class="font-bold {{ $product->stock <= $product->minimum_stock ? 'text-amber-600' : 'text-emerald-600' }}">{{ $product->stock }}</span> <small>/ {{ $product->minimum_stock }} {{ $product->unit }}</small></td><td class="p-4">{{ $product->is_active ? 'Aktif' : 'Nonaktif' }}</td><td class="p-4 text-right"><a class="font-bold text-emerald-600" href="{{ route('products.edit',$product) }}">Ubah</a></td></tr>@empty<tr><td colspan="7" class="p-8 text-center text-slate-500">Belum ada master barang.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $products->links() }}</div>@endsection
